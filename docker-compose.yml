services:
  wc-status-web:
    build: ./web
    image: wc-status-web:latest
    container_name: wc-status-web
    ports:
      - "8080:80"
    environment:
      # Sensor address; nginx proxies /api/status to it server-side so the
      # page works from any origin, including https. Set the real value via
      # a WC_DEVICE environment variable (e.g. in Portainer's stack env vars
      # or a local .env file) instead of committing it here.
      WC_DEVICE: "${WC_DEVICE:-192.168.1.60}"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
    # Reverse proxy targets: either http://<docker-host>:8080 (stable, no
    # extra config), or join the proxy's network and target the container by
    # name (http://wc-status-web:80). For the latter, uncomment below and set
    # the network name to the one your proxy stack uses:
    #
    #    networks: [default, proxy]
    #
    # networks:
    #   proxy:
    #     external: true
